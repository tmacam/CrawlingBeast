#include "parser.h"

int main (int argc, char* argv[])
{
	/* We are just tring o se if the headers are fine. */
	return 0;
}
// vim:syn=cpp.doxygen:autoindent:smartindent:fileencoding=utf-8:fo+=tcroq:
